<% provide(:title, "List subjects") %>
<h1>List subjects</h1>
<h2><%= link_to "Create new subject", new_admin_subject_path %> </h2>
<%= will_paginate %>
  <ul class="subjects">
    <%= render @subjects %>
  </ul>
<%= will_paginate %>